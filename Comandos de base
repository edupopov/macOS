# Aqui temos comandos úteis que podem ser utilizados no macos no dia a dia

# Comando para validar dns de um endereço
dig www.eduardopopovici.com
dig ns www.eduardopopovici.com
dig txt www.eduardopopovici.com

# Listar atualizações de software
softwareupdate --list-full-installers

# Qual é a versão de sistema operacional do macOS
sw_vers

# Baixar e Atualizar/Instalar versões mais atuais do macOS
softwareupdate --fetch-full-installer --full-installer-version 12.6.3

# Executar a instalação em linha de comando ou por conexão SSH
/Applications/Install\ macOS\ Monterey.app/Contents/Resources/startosinstall --agreetolicense --forcequitapps --passprompt —-rebootdelay 300

# Buscar a versão mais atualizada de seu macOS - Ex. Atualiza seu Bigsur ou Ventura - é baseado na versão instalada 
softwareupdate -a -i 

# Executar instalador de versão macOS por terminal - Monterey - Aceita os termos e licença e força encerramento de aplicações
/Applications/Install\ macOS\ Monterey.app/Contents/Resources/startosinstall --agreetolicense --forcequitapps 

# Acompanhar processo de instalação
ps aux | grep -i softwareupdade

# Identificar versão do sistema - release atual
uname -r

# Listar discos e pontos de montagem
diskutil list

# Listar o que o usuário esta usando
lsof -u usuário

# O comando uname exibe certos recursos do sistema operacional em execução em sua máquina. Por padrão, ele simplesmente exibe o nome do sistema operacional.
uname

# Mostre quem está logado e o que está fazendo.
w

# O comando who exibe uma lista de usuários atualmente conectados ao sistema local em detalhes
who

# Tempo de funcionamento do sistema (sem reboot ou desligamento)
uptime

# Mostra quantos usuários estão criados
users

# Data atual do sistema
date

# Data abreviada
date '+%a %h %d %n %T %y'

# Ativa e desativa arquivos ocultos no macOS
shift + ommand + .

# Listar impressoras
ls -la /Library/Printers

# Listar metodo de autenticação - Kerberos credentials
# REF - https://www.unix.com/man-page/osx/1/KLIST/
klist

# Conectar SSH (quando seu macOS esta com o recurso habilitado)
usuario@ip-do-macOC-na-rede

# Copiar arquivo por ssh para a pasta tmp
scp arquivo.zip usuario@ip-do-usuario:/tmp/

# Apagar hashs de conexão - útil para perda de sessão e corrompimento
rm -fvR /Users/usuario/.ssh/known_hosts

# Validar arquivo de hashs ssh
cat know_hosts  
